<form method="post" action="{{ path('offresgroupes_delete', {'id': offresgroupe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offresgroupe.id) }}">
    <button class="btn">Delete</button>
</form>

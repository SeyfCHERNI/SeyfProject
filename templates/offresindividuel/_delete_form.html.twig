<form method="post" action="{{ path('offresindividuel_delete', {'id': offresindividuel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offresindividuel.id) }}">
    <button class="btn">Delete</button>
</form>

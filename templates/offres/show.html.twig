{% extends 'base-back.html.twig' %}

{% block title %}Offres{% endblock %}

{% block body %}
    <h1>Offres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ offre.Type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offres_index') }}"class="btn btn-dark">back to list</a>

    <a href="{{ path('offres_edit', {'id': offre.id}) }}"class="btn btn-warning">edit</a>

    {{ include('offres/_delete_form.html.twig') }}
{% endblock %}
